name: "Setup Nix Environment"
description: "Install Nix and restore caches used by flake builds."
runs:
  using: composite
  steps:
    - uses: nixbuild/nix-quick-install-action@v33
      with:
        nix_conf: |
          keep-env-derivations = true
          keep-outputs = true

    - name: Restore and save Nix store
      uses: nix-community/cache-nix-action@v6
      with:
        # Cache crane's nix build dependency artifacts by Cargo.lock and flake inputs.
        primary-key: nix-${{ runner.os }}-${{ github.job }}-${{ hashFiles('Cargo.lock', '**/Cargo.toml', 'flake.nix', 'flake.lock') }}
        restore-prefixes-first-match: |
          nix-${{ runner.os }}-${{ github.job }}-
          nix-${{ runner.os }}-
        gc-max-store-size-linux: 15G

    - name: Cache Cargo dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ github.job }}-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-cargo-${{ github.job }}-
          ${{ runner.os }}-cargo-

    - name: Setup nix develop env
      shell: bash
      run: |
        echo 'use flake' > .envrc

    - uses: HatsuneMiku3939/direnv-action@v1
